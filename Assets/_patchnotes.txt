1.0

-- started project

-- added TMP Essentials & Sprite Editor packages

-- added InputActions PlayerActions & PlayerInputHandler with basic movement reading for fun

1.1

-- added Interact to action map and PlayerInputHandler

-- added Interfaces.cs & IInteractable

-- added Interactable.cs and LightSwitch.cs (LightSwitch inherits from Interactable)

-- made Player prefab. Added View and PlayerCanvas with test text for Interactables. Added PlayerView for turning the interact text on/off

1.2

-- added NormalDoor. Normal Door is an Interactable that can opens/closes depending on which side the Player Interacts with it

TODO: Change Interactable to be a constant check from the Player instead of using OnTriggerEnter/Exit for more control

1.3

-- added Inventory, ItemSO, ItemPickup, InventoryView

-- Player can now pick up items and add them to the Inventory as Interactables

-- looking at your inventory turns off movement

1.4

-- fixed an issue where Interactables wouldn't be interactable on first attempt, only subsequent. now works all the time

-- fixed Interactables displaying Interact text when not able to be interacted with

-- added SlidingDoor.cs and prefab

1.5

-- updated PlayerMovementHandler, added mouselook

-- added PlayerMiddleManager.cs to control the player directly (eg stop move/look/interact when inventory is open)

-- rejigged Interactables to use IInteract instead of layers (eg so enemies could interact with doors instead of just the player)

-- added Sprint function to movement

1.6

-- updated InventoryView to be rotational, added buttons, enabled cursor during Inventory

-- added "Locked" variants for NormalDoor and SlidingDoor

-- added LevelGenerator, LevelParts, LevelGeneratorController. Added wall, ground, hall way prefab.

1.7

-- added IsCameraLookingAtMe, EnemyAIBrain, GameObjectStateManager, two states, Idle and WalkToPlayer

-- WalkToPlayer uses NavMeshAgent to walk to the player. snap looks at the player and walks towards them when not in sight

-- added Spawner to begin spawning boards and mannequins

TODO: danni zero 3D models out correctly!!!

-- added CardboardMannequin, FollowMannequin and SpawnLocation prefabs

-- after much fucking around got NavMeshAgent and RigidBody behaving. Follow Mannequin now pathfinds walks towards the Player

1.8

-- added Health. tracks current HP, maxHP and isAlive;

-- updated Spawner to now include Player prefab. Spawner resets positions of spawned Objs when the Player dies

-- updated EnemyAI WalkToPlayer state to kill the Player when in range

-- updated Items to remove from Player Inventory and respawn on Player death

TODO: Refactor Spawner
TODO: Update Player Location / spawnedLocations to use when Player advances (checkpoint)

1.9

-- updated Spawner with more hack stuff, plz refactor

-- added LevelTriggers and LevelManager. LevelManager resets stuff when the player dies (works for doors and enemies and items)

-- continued to "fix" InteractText

TODO DONE: fix items disappearing if used and then died before reaching checkpoint
TODO: turn doors impassable after clearing checkpoint and turn off inaccessible enemies

2.0

-- fixed items disappearing on death

-- added Mirror prefab, Mirror.cs to automate mirrors (not perfect... RawImage must be flipped depending on where mirror is placed and resolution could be better?)

-- added Cinemachine (untouched so far)

2.1

-- added AAAGameManager Singleton. added Difficulty. updated AI and PlayerHealth to use Difficulty
TODO: playermiddleman is also an Instance/Singleton, change Spawner to identify Player Transform to AI

-- added HealthPlayerView to show health. toggle testing to display
TODO: Update health on start correctly

-- certains ItemSOs are now useable

-- using Items have an effect, removes the item and closes the inventory if its the last one

-- added placeholder sprites

TODO: door triggers still dont seem to update correctly first time entering?

2.2

-- updated WalkToPlayer with instant snapping for creepier feel

TODO: swapping back to rigid body from NavMeshAgent causes slight wobble

-- added PauseMenu with Idle, Pause, Options and Quit states. Player can now Pause the game by hitting escape.

-- updated Inventory and PlayerMiddleMan to turn off player abilities while paused 

2.3

-- added scant MainMenu, sceneLoader, timer

-- sceneloader loads between mainmenu and a test level

-- GameManager now starts/stops timer on sceneload

-- added EndLevel.cs

-- updated LevelManager to take an end of level trigger. Entering the end of the level now shows your rank (time + deaths) and sends back to the main menu

-- reverted PlayerMiddleManager from being a singleton. Spawner now informs AI of the Player's Transform manually...

-- started Save/Load high scores

2.4

-- reworked inventory

-- added 3d models to itemSO's

-- added options to pause menu

-- added mouse sensitivity setting

-- added volume setting

-- upgrade unity version to 6.3 LTS